# Substitute

Dieses Repository enthält nur die Webseite von Substitute.

👉 [Zur Webseite](docs/index.html)

Der Quellcode liegt in [`docs/`](docs/) und wird per GitHub Pages veröffentlicht.

> Damit die Webseite auch unter `https://gustavschroeter.github.io/Substitute` erreichbar ist, muss unter **Settings → Pages** die Source auf Branch `main`, Ordner `/docs` gesetzt werden.
